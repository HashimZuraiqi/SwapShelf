<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Forgot Password | BookSwap</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <style>
    body { background: linear-gradient(135deg, #18191E, #15181D, #0D0E10); color: #fff; }
    .auth-card { max-width: 520px; margin: 80px auto; background:#1f2126; border:1px solid rgba(255,255,255,.08); border-radius:16px; padding:28px; box-shadow:0 6px 30px rgba(0,0,0,.35);}
    .btn-gradient { background: linear-gradient(90deg, #3BB7FB, #F6B443); border: none; color: #0e0f12; font-weight: 800; }
    .text-muted { color:#b0b3b8 !important; }
  </style>
</head>
<body>
  <nav class="navbar navbar-dark custom-navbar">
    <a class="navbar-brand d-flex align-items-center" href="/"><span class="logo-icon mr-2">📚</span><span class="logo-text ml-2 gradient-text">Book<span class="swap">Swap</span></span></a>
  </nav>

  <div class="auth-card">
    <h3 class="mb-2">Forgot your password?</h3>
    <p class="text-muted mb-4">Enter the email you used to create your account. We’ll send a password reset link.</p>

    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <% if (success) { %>
      <div class="alert alert-success"><%= success %></div>
    <% } %>

    <form method="POST" action="/auth/forgot-password">
      <div class="form-group">
        <label>Email address</label>
        <input type="email" name="email" class="form-control" placeholder="Enter your email" required>
      </div>
      <button type="submit" class="btn btn-gradient btn-block">Send reset link</button>
    </form>

    <div class="mt-3">
      <a href="/login"><i class="bi bi-arrow-left"></i> Back to login</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/js/bootstrap.min.js"></script>
</body>
</html>
