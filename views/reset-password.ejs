<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Reset Password | BookSwap</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <style>
    body { background: linear-gradient(135deg, #18191E, #15181D, #0D0E10); color: #fff; }
    .auth-card { max-width: 520px; margin: 80px auto; background:#1f2126; border:1px solid rgba(255,255,255,.08); border-radius:16px; padding:28px; box-shadow:0 6px 30px rgba(0,0,0,.35);}
    .btn-gradient { background: linear-gradient(90deg, #3BB7FB, #F6B443); border: none; color: #0e0f12; font-weight: 800; }
    .text-muted { color:#b0b3b8 !important; }
  </style>
</head>
<body>
  <nav class="navbar navbar-dark custom-navbar">
    <a class="navbar-brand d-flex align-items-center" href="/"><span class="logo-icon mr-2">📚</span><span class="logo-text ml-2 gradient-text">Book<span class="swap">Swap</span></span></a>
  </nav>

  <div class="auth-card">
    <h3 class="mb-2">Set a new password</h3>
    <p class="text-muted mb-4">Choose a strong password you haven’t used before.</p>

    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <% if (!token) { %>
      <div class="mt-3">
        <a href="/auth/forgot-password"><i class="bi bi-arrow-left"></i> Request a new reset link</a>
      </div>
    <% } else { %>
      <form method="POST" action="/auth/reset-password/<%= token %>">
        <div class="form-group">
          <label>New password</label>
          <input type="password" name="password" minlength="6" class="form-control" placeholder="Enter new password" required>
        </div>
        <div class="form-group">
          <label>Confirm password</label>
          <input type="password" name="confirm" minlength="6" class="form-control" placeholder="Re-enter password" required>
        </div>
        <button type="submit" class="btn btn-gradient btn-block">Update password</button>
      </form>
      <div class="mt-3">
        <a href="/login"><i class="bi bi-box-arrow-in-right"></i> Back to login</a>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/js/bootstrap.min.js"></script>
</body>
</html>
