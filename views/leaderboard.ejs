<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monthly Leaderboard | SwapShelf</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/leaderboard.css">
    <link rel="stylesheet" href="/css/navbar-chat.css">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:700,600,400&display=swap" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
</head>
<body class="leaderboard-main">

<!-- Navigation -->
<!-- Include Standard Navbar -->
<%- include('partials/navbar') %>

<!-- Include Navbar Chat Component -->
<%- include('partials/navbar-chat') %>

<!-- Main Content -->
<div class="container-fluid px-4">
    <!-- Header -->
    <div class="leaderboard-header">
        <div class="header-content">
            <h1 class="page-title">
                <i class="bi bi-trophy-fill title-icon"></i>
                Monthly Leaderboard
            </h1>
            <p class="page-subtitle">
                Compete with fellow book lovers and climb the ranks through swaps, achievements, and community engagement
            </p>
            
            <!-- Period Selector -->
            <div class="period-selector">
                <button class="period-btn active" data-period="current">Current Month</button>
                <button class="period-btn" data-period="previous">Previous Month</button>
                <button class="period-btn" data-period="all-time">All Time</button>
            </div>
        </div>
    </div>

    <!-- Top 3 Champions Podium -->
    <div class="champions-podium">
        <div class="podium-container">
            <% if (leaderboard && leaderboard.length > 0) { %>
                <!-- 2nd Place -->
                <% if (leaderboard[1]) { %>
                <div class="podium-position second-place">
                    <div class="champion-card second">
                        <div class="rank-badge silver">2</div>
                        <div class="champion-avatar">
                            <img src="<%= leaderboard[1].avatar %>" alt="<%= leaderboard[1].displayName %>" class="avatar-image">
                            <div class="crown-icon">🥈</div>
                        </div>
                        <h3 class="champion-name"><%= leaderboard[1].displayName %></h3>
                        <div class="champion-stats">
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[1].totalPoints.toLocaleString() %></span>
                                <span class="stat-label">Points</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[1].badgeCount %></span>
                                <span class="stat-label">Badges</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[1].completedSwaps %></span>
                                <span class="stat-label">Swaps</span>
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>
                
                <!-- 1st Place -->
                <% if (leaderboard[0]) { %>
                <div class="podium-position first-place">
                    <div class="champion-card winner">
                        <div class="rank-badge gold">1</div>
                        <div class="champion-avatar">
                            <img src="<%= leaderboard[0].avatar %>" alt="<%= leaderboard[0].displayName %>" class="avatar-image">
                            <div class="crown-icon">👑</div>
                        </div>
                        <h3 class="champion-name"><%= leaderboard[0].displayName %></h3>
                        <div class="champion-stats">
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[0].totalPoints.toLocaleString() %></span>
                                <span class="stat-label">Points</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[0].badgeCount %></span>
                                <span class="stat-label">Badges</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[0].completedSwaps %></span>
                                <span class="stat-label">Swaps</span>
                            </div>
                        </div>
                        <div class="winner-glow"></div>
                    </div>
                </div>
                <% } %>
                
                <!-- 3rd Place -->
                <% if (leaderboard[2]) { %>
                <div class="podium-position third-place">
                    <div class="champion-card third">
                        <div class="rank-badge bronze">3</div>
                        <div class="champion-avatar">
                            <img src="<%= leaderboard[2].avatar %>" alt="<%= leaderboard[2].displayName %>" class="avatar-image">
                            <div class="crown-icon">🥉</div>
                        </div>
                        <h3 class="champion-name"><%= leaderboard[2].displayName %></h3>
                        <div class="champion-stats">
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[2].totalPoints.toLocaleString() %></span>
                                <span class="stat-label">Points</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[2].badgeCount %></span>
                                <span class="stat-label">Badges</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-value"><%= leaderboard[2].completedSwaps %></span>
                                <span class="stat-label">Swaps</span>
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>
            <% } else { %>
                <!-- Empty state when no leaderboard data -->
                <div class="empty-leaderboard">
                    <div class="empty-icon">
                        <i class="bi bi-trophy"></i>
                    </div>
                    <h3>No Leaderboard Data</h3>
                    <p>Start adding books and completing swaps to appear on the leaderboard!</p>
                </div>
            <% } %>
        </div>
    </div>

    <!-- Full Rankings -->
    <div class="rankings-section">
        <div class="section-header">
            <h2 class="section-title">
                <i class="bi bi-list-ol"></i>
                Full Rankings
            </h2>
            <div class="ranking-filters">
                <button class="filter-btn active" data-filter="all">All Users</button>
                <button class="filter-btn" data-filter="friends">Friends Only</button>
                <button class="filter-btn" data-filter="nearby">Nearby Users</button>
            </div>
        </div>

        <div class="rankings-list">
            <% if (leaderboard && leaderboard.length > 0) { %>
                <% leaderboard.forEach((user, index) => { %>
                    <div class="ranking-item <%= user.isCurrentUser ? 'current-user' : '' %>">
                        <div class="rank-position">
                            <span class="rank-number"><%= user.rank %></span>
                            <% if (user.isCurrentUser) { %>
                                <div class="current-user-badge">
                                    <i class="bi bi-person-check"></i>
                                    <span>You</span>
                                </div>
                            <% } %>
                        </div>
                        <div class="user-info">
                            <img src="<%= user.avatar %>" alt="<%= user.displayName %>" class="user-avatar">
                            <div class="user-details">
                                <h4 class="username"><%= user.displayName %></h4>
                                <div class="user-stats">
                                    <span class="badge-count"><%= user.badgeCount %> badges</span>
                                    <span class="divider">•</span>
                                    <span class="swap-count"><%= user.completedSwaps %> swaps</span>
                                    <span class="divider">•</span>
                                    <span class="join-date">Joined <%= user.joinDate %></span>
                                </div>
                            </div>
                        </div>
                        <div class="score-section">
                            <span class="points"><%= user.totalPoints.toLocaleString() %></span>
                            <span class="points-label">points</span>
                        </div>
                        <div class="user-actions">
                            <button class="btn btn-outline-primary btn-sm" onclick="openUserProfile('<%= user._id %>')">
                                <i class="bi bi-person"></i> View Profile
                            </button>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="empty-rankings">
                    <div class="empty-icon">
                        <i class="bi bi-list-ol"></i>
                    </div>
                    <h3>No Rankings Available</h3>
                    <p>Be the first to start competing on the leaderboard!</p>
                </div>
            <% } %>
        </div>

        <% if (leaderboard && leaderboard.length > 10) { %>
        <!-- Load More Button -->
        <div class="load-more-section">
            <button class="load-more-btn" id="loadMoreRankings">
                <i class="bi bi-arrow-down-circle"></i>
                Load More Rankings
            </button>
        </div>
        <% } %>
    </div>

    <!-- Monthly Stats -->
    <div class="monthly-stats">
        <h2 class="section-title">
            <i class="bi bi-calendar-month"></i>
            This Month's Highlights
        </h2>
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon">🔥</div>
                <div class="stat-content">
                    <h3>Most Active Day</h3>
                    <p>September 15th - 847 swaps completed</p>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">🚀</div>
                <div class="stat-content">
                    <h3>Biggest Climber</h3>
                    <p>PageTurner jumped 23 positions</p>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">🎯</div>
                <div class="stat-content">
                    <h3>New Achievers</h3>
                    <p>142 users earned their first badge</p>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/leaderboard.js"></script>
<!-- Include Simple Chat Widget -->
<!-- Include Real-Time Chat System -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<link rel="stylesheet" href="/css/realTimeChat.css">
<script src="/socket.io/socket.io.js"></script>
<script src="/js/realTimeChat.js"></script>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/js/bootstrap.min.js"></script>
<script src="/js/navbar-chat.js"></script>

</body>
</html>
